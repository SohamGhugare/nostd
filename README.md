## `nostd` implementations of core functions

### Note: These codes are for aarch64 (arm64) architecture

## Executing
1. You can import the syscalls directly from the `syscall` directory.
2. Build using Cmake

    ```bash
    cmake . # Generate Makefile
    make
    ```

